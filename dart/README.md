# zaga_payments (Dart)

```yaml
dependencies:
  zaga_payments:
    git:
      url: https://github.com/crisskimaryo/zaga-payments-sdk.git
      path: dart
```

Use it from Dart servers, or from Flutter with **test** keys only: a live key
must never ship inside an app. See the repository README for the contract.
