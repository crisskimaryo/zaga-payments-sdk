name: zaga_payments
description: Client for the Zaga Payments gateway (charges, status polling, refunds, webhook verification). 
version: 0.1.0
homepage: https://github.com/crisskimaryo/zaga-payments-sdk
repository: https://github.com/crisskimaryo/zaga-payments-sdk
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  crypto: ^3.0.3
dev_dependencies:
  test: ^1.25.0
