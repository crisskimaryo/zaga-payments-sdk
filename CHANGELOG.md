# Changelog

## 0.1.0 — 2026-09-10
- First release, extracted from the `zaga-payments` monorepo.
- TypeScript: `ZagaPayments` client (methods, products, charges, refunds, `waitForSettlement`), `verifyWebhookSignature`, `parseWebhook`.
- Dart and Python clients with the same surface.
